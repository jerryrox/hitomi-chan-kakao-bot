const HITOMI_CHAN_URL = "http://localhost:4000";

const API_BASE = `${HITOMI_CHAN_URL}`;
const API_GALLERY = `${HITOMI_CHAN_URL}/api/gallery`;
const API_ORIGINAL = `${HITOMI_CHAN_URL}/api/original/`;
const API_THUMB_BIG = `${HITOMI_CHAN_URL}/api/thumbs/big/`;
const API_THUMB_SMALL = `${HITOMI_CHAN_URL}/api/thumbs/small/`;

const VIEW_READ = `${HITOMI_CHAN_URL}/gallery/read/`;
const VIEW_WATCH = `${HITOMI_CHAN_URL}/gallery/watch/`;

module.exports = {
    HITOMI_CHAN_URL,

    API_BASE,
    API_GALLERY,
    API_ORIGINAL,
    API_THUMB_BIG,
    API_THUMB_SMALL,

    VIEW_READ,
    VIEW_WATCH
};